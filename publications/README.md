Drop publication PDFs in this folder, e.g. `deepfakes-public-safety-2026.pdf`.
Keep the filename in sync with the `pdf` field for that entry in `/data/publications.json`.

GitHub Pages serves files up to 100 MB, but for anything over a few MB consider
compressing the PDF first (mailed download links work best under ~10 MB).
